# Production Environment

## Base URL for the API. Should match Audience in JWT options on server.
REACT_APP_API_URL=https://api.productionurl.com

## Hostname used to detect if we're in production.
REACT_APP_PROD_HOSTNAME=www.productionurl.com
